<view class="check-item">
  <check-box
    disable="{{disable}}"
    select="{{select}}"
    value="{{checkBoxValue}}"
    bind:onChange="boxChange"
  />
  <view class="content">
    <slot />
  </view>
</view>
